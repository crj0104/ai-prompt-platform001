<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.course.promptplatform.mapper.TemplateUseLogMapper">

    <select id="selectDailyUsageTrend" resultType="com.course.promptplatform.model.query.UsageTrendRow">
        SELECT
            CAST(used_at AS DATE) AS statDate,
            COUNT(1) AS usageCount
        FROM template_use_log
        WHERE template_id = #{templateId}
          AND used_at <![CDATA[ >= ]]> #{startTime}
        GROUP BY CAST(used_at AS DATE)
        ORDER BY statDate ASC
    </select>
</mapper>
